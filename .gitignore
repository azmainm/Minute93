# OS
.DS_Store

# Prompt templates (private, not for public repo)
docs/prompts/

# Deployment plan (private, not for public repo)
docs/DEPLOYMENT_PLAN.md

# Seed scripts with production credentials
server/scripts/seed-all.sh

# k6 raw metrics (large files) — keep summaries and console output
k6/results/*/raw-metrics.json
